"""Quant Engine — the automated math.

Pure functions over NumPy arrays and domain dataclasses: no database, no network,
no I/O (see CLAUDE.md, layer 1). Everything here is deterministic and unit-tested
with golden-number fixtures.
"""
